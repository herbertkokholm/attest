"""Aggregation of dynamic-size LLM ensemble votes into a single include/exclude/uncertain rating."""


def aggregate(votes: list[int]) -> int:
    """Aggregate ensemble member votes into a single ordinal rating (not yet implemented)."""
    raise NotImplementedError
